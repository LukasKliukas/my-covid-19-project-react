import React from 'react';
import css from './Header.module.css';
import Container from '../UI/Container/Container';

const Header = () => {
  return <Container className={css.container}>Header</Container>;
};

export default Header;
